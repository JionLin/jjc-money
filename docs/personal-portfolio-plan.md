# 个人持仓买卖点备忘录

> 说明：
> 1. 本文基于 2026-04-26 这次对话整理，仅作为个人操作备忘录，不构成投资建议。
> 2. 持仓数量按截图做近似估算，正式下单前请以券商账户实际持仓为准。
> 3. 本文采用“金渐成视角”的框架：顺势而为、分批买卖、控制仓位、做低成本/负成本、保留现金应对波动。
> 4. `2026-05-01` 更新：`GOOGL` 已全部卖出；补充 `NVO` 清仓节奏备忘。
> 5. 本文已改为动态维护，不再在文件名中绑定日期；看“最后更新”比看文件名更重要。

---

## 一、个人前提与本次调整目标

### 已知个人约束

- 年龄：35岁
- 最大可承受回撤：约 `-30%`
- 暂不继续投入新增资金
- 这笔钱短期内暂时用不到
- 现阶段先不考虑配置 `BRK.B`
- 现阶段保留现金作为防守仓

### 本次调整目标

- 科技仓逐步收缩到以 `MSFT + NVDA` 为核心
- 先不追求一步到位把 `NVDA` 拉到 30%
- 第一阶段先把 `NVDA` 提升至约 `20%-24%`
- 后续若市场给出更深回撤，再考虑继续向 `28%-30%` 靠拢
- 保留一部分现金，避免为了目标仓位而追高

---

## 二、当前讨论中的近似持仓快照

> 以下为根据截图推算的近似值，仅用于本次计划。

| 标的 | 近似持仓 | 近似市值/仓位特征 | 当前角色 |
|---|---:|---:|---|
| `MSFT` | `52股` | 最大重仓，约总资产 `39%` 左右 | 核心骨架 |
| `META` | `13股` | 进攻型高弹性仓 | 待逐步腾挪 |
| `AMZN` | `15股` | 云+零售综合仓 | 待观察/部分腾挪 |
| `GOOGL` | `0股` | 已完成卖出 | 已退出 |
| `UNH` | `6股` | 非科技主线 | 优先腾挪 |
| `NVDA` | `8股` | 当前仓位偏低 | 重点提升 |
| `NVO` | `31股` | 医药备胎仓，计划退出 | 等反弹清仓 |
| 现金 | 原约 `$17,776` + `GOOGL` 卖出回笼资金 | 防守仓 | 保留一部分 |

---

## 三、总的资金挪动路径

### 路径A（当前采用）

- 暂不考虑买入 `BRK.B`
- 保留现金作为防守仓
- 先清理非未来核心仓位：`UNH`
- `GOOGL` 已完成清理，回笼资金暂并入现金
- `NVO` 作为备胎仓，后续利用反弹分批退出
- 暂不主动减 `MSFT`
- 暂不急于处理大部分 `META`
- 用 `UNH + 已卖出 GOOGL 回笼资金 + 部分现金` 组成 `NVDA` 专项弹药池

### `NVDA` 专项弹药池

目标弹药池：约 **`$12,000`**

来源：

- 卖出 `UNH`：剩余 `6股`，按后续卖出区间粗略估算约 `$2,310-$2,700`
- `GOOGL` 已卖出：回笼资金并入现金，实际到账以券商记录为准
- 现金划拨：约 `$8,846`

执行后，仍保留一部分现金作为后续防守和机动资金；最新可用现金请以账户实际数据为准。

---

## 四、逐只股票的买卖点整理

## 4.1 `NVDA`（英伟达）

### 当前定位

- 未来重点提升仓位的核心标的
- 第一阶段目标：先做到约 `20%-24%`
- 后续若出现更深回撤，再考虑进一步向 `28%-30%` 推进

### 买入计划（本次重点）

> 本次采用总弹药池约 `$12,000`，第一买入区间上移至 `170-175`，不在 `200+` 位置追高。

| 买入价位 | 资金占比 | 买入金额 | 估算可买股数 | 买入原因 |
|---|---:|---:|---:|---|
| `170-175` | `15%` | `$1,800` | 约 `10.3-10.6股` | 第一枪，开始建立更像样的仓位，但仍保持克制，不在高位猛冲 |
| `165` | `20%` | `$2,400` | 约 `14.5股` | 接近此前讨论的核心观察位，若跌到这里说明回撤开始像样，可加大力度 |
| `155` | `30%` | `$3,600` | 约 `23.2股` | 主力仓位区，性价比明显提升，是本轮加仓最重要的一档 |
| `145` | `20%` | `$2,400` | 约 `16.6股` | 深回调区，市场情绪更恐慌，继续利用波动扩大仓位 |
| `130` | `15%` | `$1,800` | 约 `13.8股` | 黑天鹅/极端恐慌档，保留最后子弹，防止真正大跌时没钱加 |

### 下单细化建议

- `170-175` 这一档可拆成两笔，如 `173` 和 `171`
- `165` 可一笔或两笔完成
- `155` 尽量完整保留，不轻易上移
- `145` 和 `130` 不提前动，专门留给深跌

### 买入纪律

- 若只成交到 `170-175` 就反弹，不追高补后面未成交的档位
- 真正决定长期收益率的，大概率是 `155` 和 `145` 这两档
- 若后面有新增卖出资金（如进一步处理 `AMZN`/`META`），优先补给 `145` 或 `130`

### 卖出区间（未来高位减仓参考）

| 卖出区间 | 建议动作 | 卖出原因 |
|---|---|---|
| `240-250` | 开始试探性减仓一小部分 | 到达下一段高位区，先做低成本/腾出资金 |
| `270-290` | 继续分批减仓 | 情绪升温，避免后续回撤把浮盈抹平 |
| `300+` | 进一步减仓，但保留底仓 | 不赚最后一个铜板，让利润奔跑但控制安全边际 |

---

## 4.2 `MSFT`（微软）

### 当前定位

- 当前最大核心仓
- 未来仍保留为组合骨架
- 当前仓位已经够重，暂不建议继续主动加大

### 卖出区间

| 卖出区间 | 建议卖出数量 | 卖出原因 |
|---|---:|---|
| `500-520` | `5-8股` | 到达明显高位区，先做试探性减仓，降低仓位和成本 |
| `550-580` | `8-12股` | 若市场情绪继续升温，进一步锁定利润，避免高位回撤伤害过大 |
| `600+` | 继续视情况分批减仓 | 进入更明显的情绪溢价区，遵循“不赚最后一个铜板” |

### 买入/回补区间

> 前提：只有在高位已经减过一些仓位后，才考虑回补；若仓位仍接近总资产 `40%`，即便跌到第一档也不宜兴奋加大。

| 买入区间 | 建议回补数量 | 买入原因 |
|---|---:|---|
| `380-390` | `3-4股` | 进入更合理的回补区，前提是已经高位减过仓 |
| `365-375` | `4-5股` | 更接近此前讨论的支撑区，性价比进一步提升 |
| `345-355` | `4-5股` | 深回撤区，若基本面未坏，是更舒服的加仓位置 |

### 当前对微软的原则

- 现在不是加微软的时候
- 现在更像“持有偏重，等高位减”
- 后续卖出其他股票腾出的资金，优先给 `NVDA`，不是继续砸给 `MSFT`

---

## 4.3 `META`

### 当前定位

- 高质量进攻型资产
- 但在未来“科技只收缩到微软+英伟达”为主的框架下，属于待逐步腾挪的仓位

### 卖出区间

| 卖出区间 | 建议卖出数量 | 卖出原因 |
|---|---:|---|
| `720-730` | `3股` | 第一档减仓，利用财报或情绪推动先锁一部分利润 |
| `750-780` | `4股` | 到达更热闹区间，继续做低成本、腾出资金 |
| `800-810` | `4股` | 进入更明显高位区，进一步减仓，但不必一次清空 |

### 留仓建议

- 可保留 `2股` 左右作为尾仓
- 这样既不把好票一次性卖光，也能给未来的 `NVDA` 和现金留空间

### 卖出原因

- `META` 质量很好，但波动较大
- 在“只留两只科技核心”的前提下，它不是最终承重墙
- 最适合在热闹时减，而不是在平淡区随便砍

---

## 4.4 `AMZN`

### 当前定位

- 不是坏票，但在当前框架里不是最优先保留的核心
- 更适合持有观察、逢高分批腾挪

### 卖出区间

| 卖出区间 | 建议卖出数量 | 卖出原因 |
|---|---:|---|
| `270-280` | `3股` | 进入第一档可减区，先释放部分资金 |
| `300-315` | `4-5股` | 到达更明显高位，继续做低成本并腾出仓位 |
| `330+` | 再处理剩余大头 | 若能到更高区间，不恋战，继续朝双核心结构收缩 |

### 暂不主动买入原因

- 当前框架重点是把仓位往 `MSFT + NVDA` 聚拢
- `AMZN` 更适合作为待腾挪的过渡仓，而不是新增重点仓

---

## 4.5 `GOOGL`

### 当前定位

- 已完成卖出
- 非未来最终核心
- 本轮任务已结束，不再作为当前持仓管理对象

### 当前备注

- `GOOGL` 已全部卖出
- 回笼资金优先进入现金池，再根据 `NVDA` 回撤情况决定是否分批挪用
- 后续若重新介入，需作为新开仓重新评估，不沿用旧计划

### 当前原则

- 如果已经决定收缩到两只科技核心，`GOOGL` 这种非最终核心仓就该处理干净
- 它不是坏票，只是在这套新结构里优先级不够高

---

## 4.6 `NVO`

### 当前定位

- 医药板块中的备胎仓，不是未来核心
- 当前目标不是继续摊平，而是利用反弹分批清掉
- 这类仓位最怕从“观察仓”拿成“情绪仓”

### 当前近似快照

- 近似持仓：`31股`
- 现价参考：约 `$42.24`（基于 2026-04-30 收盘附近）
- 按截图内可见交易粗算，剩余仓位摊余成本约在 `$45.06` 一线
- `2026-05-06` 为公司 2026 年一季报披露日，适合作为观察反弹强弱的时间点

### 清仓区间

| 卖出区间 | 建议卖出数量 | 卖出原因 |
|---|---:|---|
| `$45-46.5` | `10-15股` | 接近剩余成本区，先卸掉一半情绪包袱 |
| `$49-52` | `10-12股` | 接近历史买入区上沿，若财报后有反弹，这一段最适合继续做减法 |
| `$55-58` | 剩余全部 | 若能反弹到这一区间，属于体面下车，不恋战 |

### 风险纪律

- 如果财报后只反弹到 `$45` 左右就转弱，按第一档先卖，不再幻想
- 如果财报后继续走弱并有效跌破 `$40`，考虑加速清仓
- 不再新增补仓计划；这票现在的任务是退出，不是证明自己看得更久就更对

### 当前原则

- 备胎仓完成使命后，该走就走
- 不赚最后一个铜板
- 退出时优先执行纪律，而不是幻想卖在最高点

---

## 4.7 `UNH`

### 当前定位

- 非科技主线
- 与当前未来配置方向不一致
- 在本轮调整中属于优先退出标的
- 已于 `370` 一线先卖出 `2股`
- 当前剩余 `6股`，后续卖点应从“第二段修复”开始重新排布

### 卖出区间

| 卖出区间 | 建议卖出数量 | 卖出原因 |
|---|---:|---|
| `385-395` | `2股` | `370` 这第一档已经执行过，剩余仓位等修复到下一段阻力区再减 |
| `405-420` | `2股` | 本地历史锚点里，`375-422` 属于较强阻力带，进入中上沿时继续做减法 |
| `440-460` | `2股` | 若能进一步修复到下一层区间，完成剩余退出，不再恋战 |

### 调整依据

- 当前事实层：联合健康 `2026-04-21` 公布一季报，营收 `1117亿美元`、调整后 EPS `7.23 美元`，并上调全年调整后 EPS 指引至 `>18.25 美元`，说明最坏时刻较大概率已经过去。
- 市场层：4 月 30 日 Barron's 报道引用的 4 月 29 日收盘价约为 `$370.74`，显示财报后已有一轮明显修复。
- 历史锚点层：本地档案显示作者曾把 `375-422` 视为强阻力带，往上才看 `440`、`475-500` 这些更高节点。

### 卖出原因

- 不是未来的核心科技主线
- 既然目标是聚焦 `MSFT + NVDA`，这种非主线仓应该优先处理
- 与其在非核心仓里耗精力，不如腾出资金等主线回撤

### 执行纪律

- 如果反弹连 `385` 都摸不到，就不要把所有挂单继续往上抬
- 如果到了 `385-395` 仍明显放量转弱，先执行第二档，不等更高
- 真到 `440+`，重点是完成退出，不是再幻想它回 `500`

---

## 五、执行顺序建议

### 当前优先顺序

1. `UNH` 分批卖出
2. 观察 `NVO` 在财报前后是否出现可执行反弹，并按计划分批清掉
3. 确认 `GOOGL` 卖出回笼资金已进入现金池
4. 划出 `NVDA` 专项弹药池
5. 等 `NVDA` 回到 `170-175 / 165 / 155 / 145 / 130`
6. `MSFT` 等高位减仓，不主动继续加仓
7. `META`、`AMZN` 在更好的冲高区间逐步腾挪

### 不该做的事

- 不在 `NVDA 200+` 的位置为了凑目标仓位而追高
- 不因为第一档 `NVDA` 未成交就整体上移所有挂单
- 不提前动用留作防守的剩余现金
- 不在 `MSFT` 已经很重的情况下继续用新增资金猛加

---

## 六、一句话版操作地图

- `MSFT`：现在偏重，等高位减；只有跌深且前面减过仓，才考虑回补
- `NVDA`：未来重点提升仓位，但必须等回撤分批买，不追高
- `META`：好票，但在双核心收缩框架里，适合在热闹时减
- `AMZN`：持有观察，逢高逐步腾挪
- `GOOGL`：已完成卖出，回笼资金先并入现金
- `NVO`：备胎仓，按反弹节奏分批清掉
- `UNH`：非主线，优先处理
- 现金：现在就是你的防守仓，不轻易打光

---

> 以上仅为个人操作备忘录，不构成投资建议。投资有风险，入市需谨慎。
