# 阿里云 DashScope — OpenAI 兼容模式（中国北京示例端点）。
# 官方 BASE_URL 与地域列表见：https://www.alibabacloud.com/help/en/model-studio/compatibility-of-openai-with-dashscope
# 密钥：环境变量 DASHSCOPE_API_KEY（勿提交明文）。
spring:
  ai:
    openai:
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      api-key: sk-1e316880baa44b32bbbb8a4e52bc9d1d
      chat:
        options:
          # 以 Model Studio 控制台模型名为准；此处为示例
          model: qwen3.5-plus


  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${MYSQL_URL:jdbc:mysql://localhost:3306/demo?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false}
    username: ${MYSQL_USERNAME:root}
    password: ${MYSQL_PASSWORD:root}


  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 3s



lab:
  vector:
    redis:
      index-name: ${REDIS_VECTOR_INDEX:my_vector_index}
      vector-field: ${REDIS_VECTOR_FIELD:vec}
      content-field: ${REDIS_CONTENT_FIELD:content}
      top-k: ${REDIS_VECTOR_TOP_K:5}
      distance-metric: ${REDIS_DISTANCE_METRIC:COSINE}

